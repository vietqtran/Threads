export enum ThreadType {
  POST = 'post',
  REPOST = 'repost',
  POLL = 'poll'
}

export enum ThreadContentType {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
  FILE = 'file',
  POLL = 'poll',
  GIF = 'gif'
}
