export enum ThreadType {
  POST = 'post',
  REPOST = 'repost',
  SHARE = 'share'
}

export enum ThreadContentType {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
  AUDIO = 'audio',
  FILE = 'file',
  POLL = 'poll',
  GIF = 'gif'
}
